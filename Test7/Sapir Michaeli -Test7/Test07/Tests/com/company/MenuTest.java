package com.company;

/*
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
*/

import java.io.File;

public class MenuTest {



/*
    @Test
    void rightChoiceTest() {
        UserIO input = mock(UserIO.class);
        UserIO output = mock(UserIO.class);
        Main menu = new Main();
        when(input.input()).thenReturn("1").thenReturn("").thenReturn("exit");
        menu.menu();
        InOrder inOrder = inOrder(output);
        inOrder.verify(output).print("please choose:\n 1. add word \n 2. print the words\n 3. decode \n type 4 at any point to exit this program");
        inOrder.verify(output).print("enter word");

    }

    @Test
    void wrongChoiceTest() {
        UserIO input = mock(UserIO.class);
        UserIO output = mock(UserIO.class);
        Main menu = new Main();
        when(input.input()).thenReturn("4").thenReturn("").thenReturn("exit");
        menu.menu();
        InOrder inOrder = inOrder(output);
        inOrder.verify(output).print("please choose:\n 1. add word \n 2. print the words\n 3. decode \n type 4 at any point to exit this program");
        inOrder.verify(output, times(2)).print("It is not a correct choice");

    }
*/

}


